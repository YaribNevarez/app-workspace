/*
 * Empty C++ Application
 */

#include "systool.hpp"

int main(int argc, char * argv[])
{
	SystemTool app;

    int i = 0;
    for (i = 0; i < argc; i++) {
        printf("argv[%d] = %s\n", i, argv[i]);
    }

	return app.run();
}
